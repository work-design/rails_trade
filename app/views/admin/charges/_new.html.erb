<div class="ui modal" id="charge_new_modal">
  <i class="close icon"></i>
  <div class="ui header blue">
    Add New Charge
  </div>

  <div class="content">
    <%= default_form_with model: @charge, scope: :charge, url: admin_charges_path do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>

</div>
