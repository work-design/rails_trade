<% item = cart.find_item(good_id: model.id) %>

<%= form_with theme: nil, scope: 'item', url: { controller: 'trade/my/items', action: 'update', id: item.id }, method: :patch, state: 'redirect_return', data: { turbo_scroll: true }, class: 'button_to mr-1' do |f| %>
  <%= f.hidden_field :current_cart_id, value: cart.id %>
  <%= f.hidden_field :number, value: item.number - 1 %>
  <%= button_tag do %>
    <i class="fa-solid fa-xl fa-circle-minus has-text-dark-grey"></i>
  <% end %>
<% end if item && item.number > 1 %>
<%= form_with theme: nil, url: { controller: 'trade/my/items', action: 'destroy', id: item.id }, method: :delete, state: 'redirect_return', data: { turbo_scroll: true }, class: 'button_to mr-1' do |f| %>
  <%= f.hidden_field :current_cart_id, value: cart.id %>
  <%= button_tag do %>
    <i class="fa-solid fa-circle-minus fa-xl has-text-dark-grey"></i>
  <% end %>
<% end if item && item.number <= 1 %>
<%= form_with(theme: nil, scope: 'item', url: { controller: 'trade/my/items', action: 'update', id: item.id }, method: :patch, state: 'redirect_return', data: { controller: 'input', turbo_scroll: true }) do |f| %>
  <%= f.number_field :number, value: item.number, data: { action: 'change->input#form' }, class: 'has-background-light has-text-centered', style: 'width: 30px' %>
<% end if item %>
<%= form_with theme: nil, scope: 'item', url: { controller: 'trade/my/items', action: 'update', id: item.id }, method: :patch, state: 'redirect_return', data: { turbo_scroll: true }, class: 'button_to ml-1' do |f| %>
  <%= f.hidden_field :current_cart_id, value: cart.id %>
  <%= f.hidden_field :number, value: item.number + 1 %>
  <%= button_tag do %>
    <i class="fa-solid fa-xl fa-circle-plus has-text-dark-grey"></i>
  <% end %>
<% end if item %>

<%= form_with theme: nil, url: { controller: 'trade/my/items' }, state: 'redirect_return', data: { turbo_scroll: true }, class: 'button_to' do |f| %>
  <%= f.hidden_field :current_cart_id, value: cart.id %>
  <%= f.hidden_field :good_type, value: model.class_name %>
  <%= f.hidden_field :good_id, value: model.id %>
  <%= button_tag do %>
    <i class="fa-solid fa-xl fa-circle-plus has-text-dark-grey"></i>
  <% end %>
<% end unless item %>
