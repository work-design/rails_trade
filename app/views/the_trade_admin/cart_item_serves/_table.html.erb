<div>
  <table class="ui bottom attached table">
    <thead>
    <tr>
      <th></th>
      <th>Default Price</th>
      <th>Final Price</th>
      <th></th>
    </tr>
    </thead>

    <tbody id="serve_tbody">
    <% @cart_item.serve_charges.each do |serve_charge| %>
      <%= render partial: 'serve', locals: { serve_charge: serve_charge } %>
    <% end %>
    <% @cart_item.total_serve_charges.each do |serve_charge| %>
      <%= render partial: 'serve', locals: { serve_charge: serve_charge } %>
    <% end %>
    <% @cart_item.for_select_serves.each do |serve_charge| %>
      <%= render partial: 'check', locals: { serve_charge: serve_charge } %>
    <% end %>
    </tbody>
    <tfoot>
      <tr>
        <td colspan="2"></td>
        <td id="serve_subtotal"><%= @cart_item.serve.subtotal %></td>
        <td></td>
      </tr>
    </tfoot>
  </table>

</div>


