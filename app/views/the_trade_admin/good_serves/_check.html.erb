<tr id="serve_<%= serve_charge.serve_id %>">
  <td class="disabled"><%= serve_charge.item.name %></td>
  <td class="disabled"><%= serve_charge.default_subtotal %></td>
  <td><%= %></td>
  <td>
    <%= link_to 'add', add_admin_good_serves_path(@good.class.name, @good.id, serve_id: serve_charge.serve_id), method: :post, remote: true, class: 'ui green label' %>
  </td>
</tr>
