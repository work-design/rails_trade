<div class="">

  <div class="ui segment breadcrumb">
    <%= link_to 'Back', admin_payments_path, class: 'section' %>
    <div class="divider"> / </div>
    <div class="active section">Edit</div>
  </div>

  <div class="ui segment">
    <%= default_form_with model: @payment, scope: :payment, url: admin_payment_path(@payment), method: :patch, local: true do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>

</div>
