<%= render layout: 'index_table', locals: { cache_key: Card.column_names.hash } do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @cards, as: :card %>
<% end %>

<%= paginate @cards %>
