<div>
  <dt><%= Trade::Cart.human_attribute_name(:item_amount) %></dt>
  <dd class="color-success"><%= @cart.item_amount.to_money.format.html_safe %></dd>
</div>
<div>
  <dt><%= Trade::Cart.human_attribute_name(:original_amount) %></dt>
  <dd class="color-success"><%= @cart.original_amount.to_money.format.html_safe %></dd>
</div>
<div>
  <dt><%= Trade::Cart.human_attribute_name(:overall_additional_amount) %></dt>
  <dd class="color-success"><%= @cart.overall_additional_amount.to_money.format.html_safe %></dd>
</div>
<div>
  <dt><%= Trade::Cart.human_attribute_name(:overall_reduced_amount) %></dt>
  <dd class="color-success"><%= @cart.overall_reduced_amount.to_money.format.html_safe %></dd>
</div>
<div>
  <dt><%= Trade::Cart.human_attribute_name(:amount) %></dt>
  <dd class="color-success"><%= @cart.amount.to_money.format.html_safe %></dd>
</div>