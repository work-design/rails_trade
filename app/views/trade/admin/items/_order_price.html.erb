<div>
  <span style="color: grey;">Item Price: </span>
  <%= item.final_price %>
</div>
<div>
  <span style="color: grey;">Total Serve Price: </span>
  <%= item.total_serve_price %><i class="info circle icon xx_popup" style="color: grey;"></i>
  <div class="ui popup">
    <% item.total_serve_charges.each do |charge| %>
      <p><%= charge.item.name %>: <%= charge.subtotal %></p>
    <% end %>
  </div>
  <%= link_to 'edit', total_admin_item_item_serves_path(item), class: 'ui mini label' %>
</div>
<div>
  <span style="color: grey;">Promote Price: </span>
  <%= item.total_promote_price %><i class="info circle icon xx_popup" style="color: grey;"></i>
  <div class="ui popup">
    <% item.total.promote_charges.each do |charge| %>
      <p><%= charge.item.name %>: <%= charge.subtotal %></p>
    <% end %>
  </div>
</div>
<div>
  <span style="color: grey;">Final Price: </span>
  <%= item.estimate_price %>
</div>
