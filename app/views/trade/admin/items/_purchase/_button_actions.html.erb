<div class="buttons are-small">
  <%= form_with theme: 'inline', model: @purchase_order, url: { controller: 'trade/in/orders', action: 'create' }, id: 'purchase_order' do |f| %>
    <%= f.hidden_field :generate_mode %>
    <%= f.hidden_field :organ_id %>
    <%= f.submit class: 'button' %>
  <% end %>
</div>
