<td style="color: #999"><%= model.payment_id %></td>
<td><%= model.payment.type %></td>
<p>
  <span><%= model.order_amount.to_money.format.html_safe %></span>
  <span><%= model.payment_amount.to_money(model.payment.currency).format.html_safe %></span>
</p>
<p class="has-text-grey"><time data-controller="time"><%= model.created_at.to_fs(:iso8601) %></time></p>
