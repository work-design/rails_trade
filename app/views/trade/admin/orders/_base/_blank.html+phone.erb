<%= render 'blank_order' %>
<%= render 'from_address' %>
<%= render partial: 'address', locals: { model: @order.build_address } if @order.address %>

<%= render partial: 'new_item', collection: @order.items, as: :item, locals: { form: form } %>
<%= render partial: 'blank_item', locals: { item: @order.items.build, form: form } %>

<%= render partial: 'payment_strategy', locals: { form: form } %>

<%= render 'show_order' %>

<%= submit_tag '提交', class: 'button mx-3', form: 'new_order' %>
