<%= render 'top_bar' %>

<%= render 'blank_order' %>
<%= render 'from_address' %>
<%= render 'address' %>

<%= render partial: 'item', collection: @order.items, as: :item, locals: { form: form } %>
<%= render partial: 'blank_item', locals: { item: @order.items.build, form: form } %>

<% if @order.items.size > 1 %>
  <%= render partial: 'collectable', locals: { form: form } %>
<% end %>

<%= render partial: 'payment_strategy', locals: { form: form } %>

<%= render 'show_order' %>

<%= submit_tag '提交', class: 'button mx-3', form: 'new_order' %>
