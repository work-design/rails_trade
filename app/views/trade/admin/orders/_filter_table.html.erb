<table class="as-search">
  <tr>
    <th><%= Order.human_attribute_name(:payment_status) %></th>
    <td>
      <%= link_to t('.all'), filter_params(except: [:payment_status]), class: active_params(payment_status: '', active_class: 'button is-info', item_class: 'button is-light') %>
    <% Order.options_i18n(:payment_status).each do |text, value| %>
      <%= link_to text, filter_params(payment_status: value, except: [:id]), class: active_params(payment_status: value, active_class: 'button is-info', item_class: 'button is-light') %>
    <% end %>
    </td>
  </tr>
  <tr>
    <th><%= Order.human_attribute_name(:payment_type) %></th>
    <td>
      <%= link_to t('.all'), filter_params(except: [:payment_type]), class: active_params(payment_type: '', active_class: 'button is-info', item_class: 'button is-light') %>
    <% Order.options_i18n(:payment_type).each do |text, value| %>
      <%= link_to text, filter_params(payment_type: value), class: active_params(payment_type: value, active_class: 'button is-info', item_class: 'button is-light') %>
    <% end %>
    </td>
  </tr>
</table>
