<td>
  <div class="is-several">
    <% if item.good %>
      <%= image_tag item.good.logo, class: 'image is-32x32' if item.good.logo.attached? %>
      <%= link_to item.good, target: '_blank' do %>
        <span><%= item.good.name %></span>
      <% end %>
    <% end %>
    <span class="tag"><%= item.aim_i18n %></span>
  </div>
</td>
<td>
  <p>
    <span><%= item.good.price.to_money.format.html_safe if item.good %></span>
    <span class="has-text-grey">x</span>
    <span><%= item.number %></span>
  </p>
  <p><%= item.original_amount.to_money.format.html_safe %></p>
</td>
<td>
  <p><%= item.additional_amount.to_money.format.html_safe %></p>
  <p><%= item.reduced_amount.to_money.format.html_safe %></p>
</td>
<td>
  <p><time data-controller="time" data-value="<%= item.rent_start_at.to_fs(:iso8601) %>" data-format="human"><%= item.rent_start_at.to_fs(:human) %></time></p>
  <% if item.rent_estimate_finish_at %>
  <p>
    <time data-controller="time" data-value="<%= item.rent_estimate_finish_at.to_fs(:iso8601) %>" data-format="human"><%= item.rent_estimate_finish_at.to_fs(:human) %></time>
  </p>
  <% end %>
  <% if item.rent_finish_at %>
  <p class="has-text-success">
    <time data-controller="time" data-value="<%= item.rent_finish_at.to_fs(:iso8601) %>" data-format="human"><%= item.rent_finish_at.to_fs(:human) %></time>
  </p>
  <% elsif item.rent_present_finish_at %>
  <p class="has-text-success">
    <time data-controller="time" data-value="<%= item.rent_present_finish_at.to_fs(:iso8601) %>" data-format="human"><%= item.rent_present_finish_at.to_fs(:human) %></time>
  </p>
  <% end %>
  <p>
    <span><%= item.duration %></span>
    <span><%= item.good.rent_unit_i18n %></span>
  </p>
</td>
<td><%= item.amount.to_money.format.html_safe %></td>
