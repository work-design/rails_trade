<tr>
  <td><%= Trade::Order.human_attribute_name(:address_id) %></td>
  <td colspan="4">
    <%= address.area.full_name %>
    <%= address.detail %>
  </td>
  <td>
    <%= link_to({ action: 'edit', id: @order.id }, aria: { label: t('.edit.title') }, data: { turbo_frame: 'modal' }, class: 'button is-small is-borderless') do %>
      <%= svg_tag 'pencil' %>
    <% end %>
  </td>
</tr>
