<dl class="as-show as-boxed">
  <div>
    <dt><%= Trade::Order.human_attribute_name(:unreceived_amount) %></dt>
    <dd class="text-danger"><%= @order.unreceived_amount.to_money.format.html_safe %></dd>
  </div>
    <%# render partial: 'payment_wallet', collection: @order.payment_orders, as: :payment_order %>
</dl>

<%= render 'hand_payment' %>
