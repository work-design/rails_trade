<%=
  tr_tag(
    model,
    {
      href: url_for(controller: 'payment_references', action: 'new', payment_method_id: model.id),
      icon: 'images-user',
      title: t('.payment_references.new.title')
    },
    icons: ['edit', 'destroy']
  ) { yield }
%>
