<%= render 'top_bar' %>
<%= render 'filter_form' %>
<%= render partial: 'payment', locals: { model: @payment } %>

<%= render layout: 'index_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @payment_orders, as: :model %>
  <%= render partial: 'order_tbody', layout: 'order_tr', collection: @payment.pending_push_orders, as: :model %>
<% end %>
