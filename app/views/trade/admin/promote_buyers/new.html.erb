<div>

  <div class="ui segment breadcrumb">
    <%= link_to 'Back', admin_promote_buyers_path(request.GET), class: 'section' %>
    <i class="fas fa-angle-right divider"></i>
    <div class="active section">Add</div>
  </div>

  <div class="ui segment">
    <%= render 'form' %>
  </div>

</div>
