<td><%= model.type_i18n %></td>
<td>
  <p><%= model.min %></p>
  <p><%= model.max %></p>
</td>
<td>
  <p>
    <%= form_with theme: 'switch', scope: 'promote_charge', model: model, url: { action: 'update', id: model.id }, data: { controller: 'input' } do |f| %>
      <%= f.checkbox :contain_min, data: { action: 'input#submit' }, id: nil %>
    <% end %>
  </p>
  <p>
    <%= form_with theme: 'switch', scope: 'promote_charge', model: model, url: { action: 'update', id: model.id }, data: { controller: 'input' } do |f| %>
      <%= f.checkbox :contain_max, data: { action: 'input#submit' }, id: nil %>
    <% end %>
  </p>
</td>
<td><%= model.parameter %></td>
<td><%= model.base_price.to_money.format.html_safe %></td>
<td><pre><%= model.extra.to_format_yaml %></pre></td>
