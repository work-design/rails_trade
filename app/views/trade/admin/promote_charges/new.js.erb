if ($('.ui.modals').length === 0) {
  $('body').append('<%= j(render 'new') %>');
} else {
  $('.ui.modals').html('<%= j(render 'new') %>');
}


$('#charge_new_modal').modal('setting', {autofocus: false}).modal('show');
remote_js_load(<%= raw remote_js_load %>)
