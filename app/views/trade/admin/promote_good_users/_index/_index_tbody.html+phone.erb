<p><%= model.good_type_i18n %></p>
<p><%= model.promote.name %></p>
<p>
  <span class="has-text-grey">使用次数：</span>
  <span><%= model.use_limit %></span>
<p>
  <time data-controller="time"><%= model.effect_at&.to_fs(:iso8601) %></time>
  <span>~</span>
  <time data-controller="time" class="has-text-success"><%= model.expire_at&.to_fs(:iso8601) %></time>
</p>
