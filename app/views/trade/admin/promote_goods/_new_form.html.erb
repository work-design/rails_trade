<%= form_with model: @promote_good, url: { action: 'create' } do |f| %>
  <%= render 'error_messages', target: @promote_good  %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= f.submit %>
<% end %>
