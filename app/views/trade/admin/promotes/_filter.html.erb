<dl>
  <dt><%= Promote.human_attribute_name(:scope) %></dt>
  <dd>
    <%= link_to t('.all'), filter_params(except: [:scope]), class: active_params(scope: '', active_class: 'ui basic blue button', item_class: 'ui basic white button') %>
    <% Promote.options_i18n(:scope).each do |text, value| %>
    <%= link_to text, filter_params(scope: value), class: active_params(scope: value, active_class: 'ui basic blue button', item_class: 'ui basic white button') %>
    <% end %>
  </dd>
</dl>
