<div class="breadcrumb has-succeeds-separator">
  <ul>
    <li>
    <% if params[:rentable_type] == 'Ship::BoxHost' %>
      <%= link_to t('ship.admin.box_hosts.index.title'), { controller: 'ship/admin/box_hosts', anchor: "tr_#{params[:rentable_id]}" } %>
    <% end %>
    </li>
    <li class="is-active"><%= t('.title') %></li>
  </ul>
</div>
