if ($('.ui.modals').length === 0) {
  $('body').append('<%= j(render 'edit') %>');
} else {
  $('.ui.modals').html('<%= j(render 'edit') %>');
}

$('#charge_edit_modal').modal('setting', {autofocus: false}).modal('show');