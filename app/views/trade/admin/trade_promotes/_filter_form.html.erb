<%= form_with theme: 'search', model: TradePromote.new do |f| %>
  <div class="fields">
    <%= f.text_field :name %>
    <%= f.submit %>
    <%= link_to t('.clear'), filter_params(except: [:name]), class: 'button is-light' %>
  </div>
<% end %>
