<% if params[:return_state].present? %>
  <%= turbo_stream.visit url: url_for(**StateUtil.decode(params[:return_state])) %>
<% else %>
  <%= turbo_stream.visit url: url_for(controller: 'orders', action: 'show', id: @order.id) %>
<% end %>
