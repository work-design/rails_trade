<div class="box mx-3">
  <div class="box-header"><%= @item.organ&.name %></div>
  <div class="weui-panel__bd">
    <div class="media">
      <div class="media-left">
        <%= image_tag @item.good.logo, class: 'image is-64x64' %>
      </div>
      <div class="media-content">
        <strong class="weui-media-box__title"><%= @item.good_name %></strong>
      </div>
    </div>
  </div>
</div>

<div class="box mx-3">
  <%= render partial: 'item', locals: { model: @item } %>
</div>

<%= render partial: 'hold', collection: @item.holds, as: :model %>

