<%= link_to({ controller: 'trade/my/orders', action: 'add', good_id: purchase.id, good_type: purchase.class_name }, class: 'column is-one-third has-background-light is-flex-direction-column has-text-grey has-text-centered') do %>
  <div class="header"><%= purchase.title %></div>
  <div class="description"><%= purchase.price.to_money.format(html_wrap: true).html_safe %></div>
<% end %>
