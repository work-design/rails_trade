<div id="item_<%= model.id %>" class="<%= model.status_expired? ? 'box mx-3 has-background-light' : 'box mx-3' %>">
<% if model.aim_rent? %>
  <%= render partial: 'good_rent', locals: { model: model } %>
<% else %>
  <%= render partial: 'good', locals: { model: model } %>
<% end %>
</div>
