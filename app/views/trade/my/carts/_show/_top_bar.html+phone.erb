<%= render 'top_bar_left' do %>
  <span class="bar-title">
    <span class="mr-1"><%= t('.title') %></span>
    <% if @cart.member %>
      <span class="tag is-badge">
        <i class="fa-solid fa-building-user"></i>
        <span><%= @cart.member.organ.name %></span>
      </span>
    <% end %>
    <% if @cart.aim != 'use' %>
      <span class="tag is-badge">
        <i class="fa-solid fa-hand-holding-box"></i>
        <span><%= @cart.aim_i18n %></span>
      </span>
    <% end %>
  </span>
  <%= link_to({ action: 'admin' }, class: 'px-4') do %>
    <%= svg_tag 'ellipsis-vertical' %>
  <% end %>
<% end %>