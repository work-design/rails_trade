<tbody>
  <tr>
    <td></td>
    <td><%= Trade::Cart.human_attribute_name(:retail_price) %></td>
    <td colspan="2"></td>
    <td><%= @cart.retail_price.to_money.format.html_safe %></td>
  </tr>
  <tr class="has-text-danger">
    <td></td>
    <td><%= Trade::Cart.human_attribute_name(:discount_price) %></td>
    <td colspan="2"></td>
    <td><%= @cart.discount_price.to_money.format.html_safe %></td>
  </tr>
  <tr>
    <td></td>
    <td><%= Trade::Cart.human_attribute_name(:bulk_price) %></td>
    <td colspan="2"></td>
    <td><%= @cart.bulk_price.to_money.format.html_safe %></td>
  </tr>
  <tr class="has-text-danger">
    <td></td>
    <td><%= Trade::Cart.human_attribute_name(:overall_reduced_amount) %></td>
    <td colspan="2"></td>
    <td><%= @cart.overall_reduced_amount.to_money.format.html_safe %></td>
  </tr>
  <tr>
    <td></td>
    <td><%= Trade::Cart.human_attribute_name(:overall_additional_amount) %></td>
    <td colspan="2"></td>
    <td><%= @cart.overall_additional_amount.to_money.format.html_safe %></td>
  </tr>
  <tr>
    <td></td>
    <td><%= Trade::Cart.human_attribute_name(:amount) %></td>
    <td colspan="2"></td>
    <td><strong><%= @cart.amount.to_money.format.html_safe %></strong></td>
  </tr>
</tbody>
