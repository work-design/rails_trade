<%= content_for(:wechat_js) do %>
  <%# wechat_config_js debug: false, apis: ['openAddress'] if defined?(:wechat_config_js) -%>
<% end %>

<div class="weui-panel weui-panel_access">
  <div class="weui-panel__hd"><%= t('.title') %></div>
  <div class="weui-panel__bd">
    <%= link_to({ controller: 'profiled/my/addresses', return_to: url_for(controller: 'trade/my/carts', action: 'show') }, class: 'weui-cell weui-cell_access') do %>
      <div class="weui-cell__bd">
        <% if current_cart.address %>
          <p><%= current_cart.address.contact %></p>
          <p><%= current_cart.address.detail %></p>
        <% else %>
          <p>选择地址</p>
        <% end %>
      </div>
      <div class="weui-cell__ft"></div>
    <% end %>
    <%= render partial: 'trade_item', collection: @trade_items, as: :model %>
  </div>
</div>
