<div class="media">
  <%= link_to({ action: 'show', id: model.id }, data: { controller: 'empty' }, class: 'media-left') do %>
    <%= image_tag model.good.logo, class: 'image is-64x64' if model.good && model.good.logo.attached? %>
  <% end %>
  <div class="media-content">
    <%= yield %>
  </div>
  <div class="media-right">
    <%= button_to({ action: 'actions', id: model.id }, class: 'button is-borderless') do %>
      <%= svg_tag 'ellipsis-vertical' %>
    <% end %>
  </div>
</div>
