<%= turbo_stream.replace 'cart_nav', partial: 'cart_nav', locals: { cart: @item.current_cart, cart_con: 'trade/my/carts' } %>
<%= turbo_stream.replace "cart_#{@item.good_id}", partial: 'add_cart', locals: { cart: @item.current_cart, model: @item.good, item_con: 'trade/my/items' } %>
