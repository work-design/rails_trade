<%= render 'top_bar' %>

<div class="weui-panel">
  <div class="weui-panel">
    <table class="table is-fullwidth is-borderless is-striped as-show">
      <tbody>
        <%= render 'show_table' %>
      </tbody>
    </table>
  </div>
</div>
