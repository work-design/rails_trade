<div class="media">
  <div class="media-left">
    <%= image_tag item.good.logo, class: 'image is-64x64' if item.good.logo&.attached? %>
  </div>
  <div class="media-content">
    <h4 class="weui-media-box__title"><%= item.good_name %></h4>
    <p class="text-danger"><%= item.amount.to_money.format.html_safe %></p>
  </div>
</div>
