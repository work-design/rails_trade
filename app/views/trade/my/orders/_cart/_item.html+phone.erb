<div class="media">
  <% if item.good.logo.attached? %>
    <div class="media-left">
      <%= image_tag item.good.logo.variant(resize_to_limit: [64, 64]), class: 'image is-64x64' %>
    </div>
  <% end %>
  <div class="media-content">
    <p>
      <span><%= item.good_name %></span>
      <% if item.dispatch.present? %>
        <span class="tag is-info size-7"><%= item.dispatch_i18n %></span>
      <% end %>
      <% if item.desk %>
        <span class="tag is-info size-7"><%= item.desk.name %></span>
      <% end %>
    </p>
    <p>
      <span class="text-danger"><%= item.single_price.to_money.format.html_safe %></span>
      <span>x</span>
      <span class="size-7"><%= item.number %></span>
    </p>
    <p><%= item.member&.name %></p>
  </div>
</div>
