<div class="tabs is-toggle is-toggle-rounded as-navbar is-sticky flex-shrink-0 mt-3 is-fullwidth" style="">
  <ul>
  <% [:init, :produced, :done, :canceled].each do |status| %>
    <li class="<%= active_helper(state: status, active: 'is-active') %>"><%= link_to Trade::Order.enum_i18n(:state, status), { state: status }, data: { turbo_action: 'replace' } %></li>
  <% end %>
  </ul>
</div>
