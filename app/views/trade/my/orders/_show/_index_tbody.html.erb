<td>
  <p><%= model.good_name %></p>
  <%# link_to model.good&.name, model, action: 'show', id: model.good_id %>
  <p>
    <% if model.aim == 'rent' %>
      <%= model.aim_i18n %>
    <% end %>
  </p>
</td>
<td><%= link_to '订单流程', { controller: 'items', action: 'show', order_id: @order.id, id: model.id } %></td>
<td>
  <span><%= model.single_price.to_money.format.html_safe %></span>
  <i class="fa-solid fa-times has-text-grey"></i>
  <span><%= model.number.to_human %></span>
</td>
<td><%= model.original_amount.to_money.format.html_safe %></td>
<td>
  <p><%= model.additional_amount.to_money.format.html_safe %></p>
  <p><%= model.reduced_amount.to_money.format.html_safe %></p>
</td>
<td class="has-text-danger"><%= model.amount.to_money.format.html_safe %></td>
<td><%= model.note %></td>
