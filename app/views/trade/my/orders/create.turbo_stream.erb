<%= turbo_stream.append 'body' do %>
  <% if @order.trade_items.pluck(:aim).all?('rent') %>
    <%= render partial: 'visit', locals: { url: url_for(action: 'index') } %>
  <% else %>
    <%= render partial: 'visit', locals: { url: url_for(action: 'payment_types', id: @order.id) } %>
  <% end %>
<% end %>
