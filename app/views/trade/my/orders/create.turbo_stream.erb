<% if @order.items.pluck(:aim).all?('rent') %>
  <%= turbo_stream.visit url: url_for(action: 'payment_frozen', id: @order.id) %>
<% else %>
  <%= turbo_stream.visit url: url_for(action: 'payment_types', id: @order.id) %>
<% end %>
