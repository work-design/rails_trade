<%= render 'filter_table' %>

<%= render layout: 'index_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @order.trade_items, as: :model %>
  <%= render partial: 'promote_tbody', layout: 'promote_tr', collection: @order.cart_promotes, as: :model %>
<% end %>
