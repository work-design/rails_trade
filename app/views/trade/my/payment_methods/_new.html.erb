<div class="ui modal">
  <i class="fas fa-times close"></i>
  <div class="ui header">
    Add Payment Method
  </div>

  <div class="content">
    <%= form_with model: [:my, @payment_method] do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>

</div>
