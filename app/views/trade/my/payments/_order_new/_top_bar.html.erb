<div class="as-bar">
  <%= link_to({ controller: 'orders', action: 'payment_types', id: @order.id }, class: 'px-4') do %>
    <%= svg_tag 'angle-left', kind: 'regular' %>
  <% end %>
  <div class="bar-title"><%= @order.uuid %></div>
  <i class="px-5"></i>
</div>
