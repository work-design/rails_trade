<%= render 'top_bar' %>

<% if @promote_good_users.blank? && @promote_good_types.blank? %>
<div class="has-page-centered">
  <p class="has-text-centered color-grey">暂无优惠券</p>
</div>
<% else %>
  <%= render layout: 'index_list' do %>
    <%= render partial: 'index_item', layout: 'index_item_raw', collection: @promote_good_users, as: :model %>
  <% end %>

  <%= render layout: 'index_list' do %>
    <%= render partial: 'index_item', layout: 'index_item_raw', collection: @promote_good_types, as: :model %>
  <% end %>
<% end %>
