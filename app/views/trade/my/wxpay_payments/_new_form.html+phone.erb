<%= form_with theme: 'weui', model: @payment, url: { action: 'create' }, state: 'return', data: { default_valid_css_value: 'has-text-danger' } do |f| %>
  <%= render partial: 'keyboard_form', locals: { f: f, id: 'wxpay_payment_total_amount' } %>
<% end %>
